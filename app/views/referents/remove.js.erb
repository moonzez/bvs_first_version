referent_err = "<%= generate_errors_string(@referent) %>"
if (referent_err.length > 0)
  alert(referent_err)
else {
  tr = $("tr#<%= @referent.id %>").remove()
  alert("Referent <%= @referent.full_name %> wurde gelöscht")
}
